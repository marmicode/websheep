import { createAction } from '@ngrx/store';

export const showHackAssistant = createAction('[Layout] Show hack assistant');

export const hideHackAssistant = createAction('[Layout] Hide hack assistant');
